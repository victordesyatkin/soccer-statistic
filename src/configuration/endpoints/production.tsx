const endpoints = {
  fetchLeagues: '/leagues',
  fetchCountries: '/countries',
  fetchContinents: '/continents',
};

// eslint-disable-next-line import/prefer-default-export
export { endpoints as production };
